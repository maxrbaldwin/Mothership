var store = require('./store');

var reports = function(server) {
  // if there is a server value, look up that server value in the store object.
  // If it exists get reports from that server.
  // If not, console log, not set.
};

module.exports = reports;
